extends video_layout

block content
  h1= title

  ul
    each con in con_list
      li
        a(href=con.url) 
        p #{con.date}, #{con.title}, #{con.description}

    else
      li There are no cons.
